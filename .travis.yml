language: php

php:
  - 7.0
  - 7.1
  - 7.2

before_script:
  - composer install

script:
  - php bin/console doctrine:database:create --env=test
  - php bin/console doctrine:fixtures:load --no-interaction
  - bin/phpunit --no-coverage --colors=always --stderr